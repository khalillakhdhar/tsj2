/**
* Ajoute deux nombres.
* @param x Le premier nombre à ajouter.
* @param y Le deuxième nombre à ajouter.
* @returns La somme des deux nombres.
*/
function additionner(x: number, y: number): number {
    return x + y;
    }